# Number Standards

XVCA uses Big-Endian numbers. In assembly language and documentation, the prefix $ indicates that a number is in hexadecimal, while % indicates binary. By default, numbers are in base 10